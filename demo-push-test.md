# Demo push test

Temporary file used to trigger a credential prompt on the first
terminal-initiated `git push`, so the GitHub Personal Access Token
gets stored in Git Credential Manager.

Safe to delete once the token is saved.
